https://github.com/shubhanshurav/Interview-Preparation-Notes/tree/master [DSA Sheet]

https://github.com/shubhanshurav/Interview-Preparation-Notes.git [DSA Sheet]

https://www.geeksforgeeks.org/javascript/javascript-interview-questions/ [javascript Questions]

https://prepinsta.com/data-structures/?loggedin=1778845459452 [DAS Questions]

Here's a comprehensive breakdown of **Data Structures and Algorithms (DSA)** topics, organized by category and difficulty level. This roadmap is ideal for students, developers, and anyone preparing for coding interviews or competitive programming.

## 📚 Data Structures

Interview-ready summary 🧠

_____________________________________]]Time Complexity]]_______________________________________

1 for loop → O(n)
2 for nested loops → O(n²)
2 for loops sequentially → O(n) two for loop differnt different used so n + n = Big O of 2n
3 for nested loops → O(n³)
______________________________________________________________________________________________

}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}
}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}ASKING BY INTERVIEWER IN  INTERVIES}}}}}}}}}}}}}}}}}}}}}}
}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}
}}}what is nginx
______________________________________________________________________________________________
}}}what is load balanced?
-> Yes, this is a good simple explanation. You can say it like this in interview:

“Load balancing is the process of balancing the client requests across multiple servers so that the complete load does not come on a single server. In this, all client requests are distributed to multiple servers, and every client can properly interact with the application and get a proper response without waiting. This process is called load balancing.”
______________________________________________________________________________________________
}}}how to share data between two components
______________________________________________________________________________________________

}}}What is Difference between linked list and arraylist and array
Array and Linked List are both data structures used to store data.

Array stores data in a continuous memory location and indexing-wise, so retrieving data is very fast because we can directly access elements using their index.

Linked List consists of nodes, and each node contains data and the address/reference of the next node.

Arrays are best for accessing or retrieving data because of indexing. However, insertion and deletion in the middle can be costly because elements need to be shifted.

Linked Lists are good for insertion and deletion operations because we only need to change the node references. However, accessing or searching data is slower because we need to traverse the nodes one by one.

-----------------------------------------------------------------------------------------------------------------------------------------
| Feature          | Array                                                  | Linked List                                               |
| ---------------- | ------------------------------------------------------ | --------------------------------------------------------- |
| **Storage**      | Stores elements in **contiguous memory (index-based)** | Stores elements in **non-contiguous memory (node-based)** |
| **Structure**    | Only stores **data**                                   | Each node has **data + address (pointer to next node)**   |
| **Access**       | **Fast (O(1))** using index (e.g., arr[2])             | **Slow (O(n))** – must traverse from head                 |
| **Insertion**    | **Difficult/Costly** (shifting elements required)      | **Easy** (just change pointers)                           |
| **Deletion**     | **Costly** (shifting needed)                           | **Easy** (update links)                                   |
| **Memory Usage** | Less memory (no extra pointer)                         | More memory (extra pointer for each node)                 |
| **Size**         | Fixed size (in most cases)                             | Dynamic (can grow/shrink anytime)                         |
| **Types**        | 1D, 2D, Multi-dimensional arrays                       | Singly, Doubly, Circular linked lists                     |
-----------------------------------------------------------------------------------------------------------------------------------------
______________________________________________________________________________________________
}}}How to implement linked list 
-> “I have mostly worked with arrays in my application development, so I haven’t implemented linked lists much in real projects. However, I understand the concept clearly.
->A linked list consists of nodes where each node contains two parts: data and a reference (address) to the next node. The first node is called the head.
->To implement it, we first create a node structure, then maintain a head pointer. For operations like insertion or deletion, we update the links between nodes instead of shifting elements like in arrays.”
______________________________________________________________________________________________
}}}what is single linked list
->A singly linked list is a type of linked list where each node contains two parts: data and address to the next node.
The first node is called the head, and the last node points to null, which indicates the end of the list.
______________________________________________________________________________________________
}}}what is doubly linked list 
-> A doubly linked list is a type of linked list where each node contains three parts: A address to the previous node, the data, and a address to the next node.
______________________________________________________________________________________________
}}}differanance between the single link list & double liked list 
-> A singly linked list contains nodes with two parts: data and a pointer to the next node, so it can be traversed only in one direction.
-> A doubly linked list contains nodes with three parts: a pointer to the previous node, data, and a pointer to the next node, so it can be traversed in both directions.
______________________________________________________________________________________________
}}}circular linked list.
-> Circular linked list is a type of linked list where the last node (tail) is connected to the head node instead of null.
______________________________________________________________________________________________
}}}reverse array without inbuild method
-> 

const arr = [12,34,78,3,9,5,20];

console.log("Before Reverse :- ",arr);

function reverseArray(arr){
	let i = 0;
	let j = arr.length - 1;
	while(i<j){
		let temp = arr[i];
		arr[i] = arr[j];
		arr[j] = temp;
		i++;
		j--;
	}
	return arr;
}

console.log("After Reverse :- ",reverseArray(arr));
______________________________________________________________________________________________ 
}}}prime number find 
->
function isPrime(num){
  if(num<=1) return false;
  for(let i=2;i<num;i++){
    if(num%2===0){
      return false;
    }
  }
  return true;
}

console.log(isPrime(0));
console.log(isPrime(1));
console.log(isPrime(2));
console.log(isPrime(3));
console.log(isPrime(4));
console.log(isPrime(5));
console.log(isPrime(6));
console.log(isPrime(7));
console.log(isPrime(10));
______________________________________________________________________________________________
}}}how many times number here in array find the countes of same number present in array 
->// }}}how many times number here in array find the countes of same number present in array 
// ->

const arr = [12,23,34,45,12,34,23,89,54,90,12];

function countesOfSameNumber(arr){
  let count = {};
  for(let i=0;i<arr.length;i++){
    let num = arr[i];
    if(count[num]){
      count[num]++;
    }
    else{
      count[num] = 1;
    }
  }
  return count;
}

console.log(countesOfSameNumber(arr));
______________________________________________________________________________________________


}}}bubble sort 1D,2D
const arr = [[12, 23, 90, 78, 4, 89], [45, 99, 1, 8, 87], [21, 32, 2]];

console.log('Before Sorted :- ', arr);

function sortArray(arr) {
  for (let i = 0; i < arr.length; i++) {
    for (let j = 0; j < arr.length; j++) {
      if (arr[j] > arr[j + 1]) {
        let temp = arr[j];
        arr[j] = arr[j + 1];
        arr[j + 1] = temp;
      }
    }
  }
  return arr;
}

function sort2DArray(arr) {
  const newArray = [];
  for (let i = 0; i < arr.length; i++) {
    newArray.push(sortArray(arr[i]));
  }
  return newArray;
}

console.log('After Sorted :- ', sort2DArray(arr));
______________________________________________________________________________________________
}}}find maximum number and smallest number in array 
const arr = [23,45,89,90,12,78,44];

function findMaximumNumber(arr){
  let max = arr[0];
  for(let i=1;i<arr.length;i++){
    if(arr[i]<max){
      max = arr[i];
    }
  }
  return max;
}

console.log("Maximum",findMaximumNumber(arr));
______________________________________________________________________________________________
}}}console.log(null+1)//1
______________________________________________________________________________________________
}}}console.log(undefined+1)//NaN
______________________________________________________________________________________________
}}}console.log(null+null)//0
______________________________________________________________________________________________
}}}console.log(null-null)//0
______________________________________________________________________________________________
}}}console.log(null)//null
______________________________________________________________________________________________
}}}console.log(null+0)//0
______________________________________________________________________________________________
}}}console.log(undefined+undefined)//NaN
______________________________________________________________________________________________
}}}console.log(null+undefined)//NaN
______________________________________________________________________________________________
}}}console.log(undefined+null)//NaN
______________________________________________________________________________________________
}}}console.log(null)//null
______________________________________________________________________________________________
}}}console.log(undefined)//undefined
______________________________________________________________________________________________
}}}console.log(this)//{}
______________________________________________________________________________________________
}}}console.log(this.name)//undefined
______________________________________________________________________________________________
}}}console.log(this.age)//undefined
______________________________________________________________________________________________
}}}console.log(this.name.age)//TypeError: Cannot read properties of undefined (reading 'age')
______________________________________________________________________________________________
}}}console.log([] instanceof Array);//true
}}}console.log([] instanceof Object);//true
}}}console.log({} instanceof Array);//false
}}}console.log({} instanceof Object);//true
}}}console.log([] instanceof {});//TypeError: Right-hand side of 'instanceof' is not callable
______________________________________________________________________________________________
find second highet salary
______________________________________________________________________________________________
what is recurson and factorial 
______________________________________________________________________________________________
how to find the current url in angular
______________________________________________________________________________________________
what is differance between active router and router module
______________________________________________________________________________________________


}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}}

### 🔹 Linear Data Structures
- **Arrays** :- 
  1.An array is a linear data structure that stores elements in continuous memory locations.
  2.It is an index-based data structure, so elements are accessed using an index number.
  👉 Arrays can store different data types (numbers, strings, objects, etc.)

  In most languages (C, C++, Java):
  👉 Arrays store same data type only
______________________________________________________________________________________________

- **Linked Lists** (Singly, Doubly, Circular)
    1.A linked list is a collection of nodes, where each node contains data and the address of the next node.

    Singly Linked Lists :- A singly linked list is a linked list where each node contains data and the address of the next node only.

    Doubly Linked List :- A doubly linked list is a linked list where each node contains data, the address of the previous node, and the address of the next node.

    Circular Linked List :- A circular linked list is a linked list where the last node points back to the first node instead of null
______________________________________________________________________________________________
- **Stacks**
    A stack is a linear data structure that stores data in LIFO or FILO order
    (Last In First Out or First In Last Out).
    In stack, we use Push operation for adding the element and Pop operation for removing the element.
    If we try to remove an element from an empty stack, it is called stack underflow.
    If the stack limit exists and we try to add one more element, it is called stack overflow.
______________________________________________________________________________________________
- **Queues** (Simple, Circular, Priority Queue, Deque)
    Queue is also the linear data structure that stored the data like LILO or FIFO Order (Last in Last out OR First in First Out);
    In a queue, insertion of elements is done from the end, and removal of elements is done from the front.

    Queue also the some types :- 
    1️⃣ Simple Queue
    In a simple queue, elements are inserted from the end and removed from the front.

    2️⃣ Circular Queue
    In a circular queue, the last position is connected back to the first position to efficiently use memory.

    3️⃣ Priority Queue
    In a priority queue, elements are removed based on priority, not on insertion order.

    4️⃣ Deque (Double Ended Queue)
    In a deque, insertion and deletion of elements can be done from both front and end.

_____________________________________________________________________________________
### 🔹 Non-Linear Data Structures
- **Trees**
  - Binary Tree
  - Binary Search Tree (BST)
  - AVL Tree
  - Segment Tree
  - Fenwick Tree (Binary Indexed Tree)
  - Trie (Prefix Tree)
  - N-ary Tree
- **Graphs**
  - Directed & Undirected
  - Weighted & Unweighted
  - Representations: Adjacency Matrix/List

### 🔹 Hashing
- Hash Tables / Hash Maps
- Collision Resolution Techniques (Chaining, Open Addressing)

### 🔹 Heaps
- Min Heap / Max Heap
- Heapify and Priority Queues

### 🔹 Disjoint Set (Union-Find)
- Union by Rank
- Path Compression

---

______________________________________________________________________________________________

## ⚙️ Algorithms

### 🔸 Sorting Algorithms
- Bubble Sort
- Selection Sort
- Insertion Sort
- Merge Sort
- Quick Sort
- Heap Sort
- Counting Sort
- Radix Sort
- Bucket Sort

### 🔸 Searching Algorithms
- Linear Search
- Binary Search
- Ternary Search
- Exponential Search
- Interpolation Search

### 🔸 Recursion & Backtracking
- Tower of Hanoi
- N-Queens Problem
- Sudoku Solver
- Subset/Permutation Generation

### 🔸 Divide and Conquer
- Merge Sort
- Quick Sort
- Binary Search
- Closest Pair of Points

### 🔸 Dynamic Programming (DP)
- Memoization vs Tabulation
- Longest Common Subsequence (LCS)
- Longest Increasing Subsequence (LIS)
- 0/1 Knapsack
- Matrix Chain Multiplication
- Coin Change
- Edit Distance

### 🔸 Greedy Algorithms
- Activity Selection
- Huffman Coding
- Fractional Knapsack
- Kruskal’s and Prim’s Algorithms

### 🔸 Graph Algorithms
- Depth-First Search (DFS)
- Breadth-First Search (BFS)
- Dijkstra’s Algorithm
- Bellman-Ford Algorithm
- Floyd-Warshall Algorithm
- Topological Sort
- Minimum Spanning Tree (MST)
- Tarjan’s and Kosaraju’s Algorithms (SCC)
- Union-Find Applications

### 🔸 String Algorithms
- Pattern Matching (KMP, Rabin-Karp)
- Z-Algorithm
- Trie Operations
- Suffix Arrays and Trees
- Longest Palindromic Substring

### 🔸 Bit Manipulation
- Bitwise Operators
- Bitmasking
- Counting Set Bits
- XOR Tricks

### 🔸 Mathematical Algorithms
- GCD, LCM (Euclidean Algorithm)
- Sieve of Eratosthenes
- Modular Arithmetic
- Fast Exponentiation
- Prime Factorization

---

## 🧠 Advanced Topics
- Segment Trees with Lazy Propagation
- Binary Lifting (LCA)
- Heavy-Light Decomposition
- Mo’s Algorithm
- Persistent Data Structures
- Suffix Automaton
- Convex Hull
- Number Theoretic Transform (NTT)
- Dynamic Connectivity

---

--------------------------------------------------------------------------------------------------------------------------------


Here’s a **complete topic-wise breakdown of 25+ interview questions per Data Structures and Algorithms (DSA) topic**, organized from **simple to advanced**. This is perfect for mastering DSA for interviews and competitive programming.

---

_________________________________________________________________________________
https://www.codinn.dev/tricky-javascript/es6789-code-snippets-interview-questions


## 📌 Arrays (25+ Questions)

    -]] Find First Non-repeating char from string 

    let str = 'angular';

    function findFirstChar(str){
    for(let i=0;i<str.length;i++){
        let count = 0;
        for(let j=0;j<str.length;j++){
            if(str.charAt(i)===str.charAt(j)){
                count++;
            }
        }
        if(count===1){
            console.log(str[i]);
            break;
        }
    }
    }

    findFirstChar(str);
------------------------------------------------------------------
- ]]Find duplicate number

  const arr = [23,12,23,42,78,90,42];
  for(let i=0;i<arr.length;i++){
      let duplicate = arr[i];
      for(let j=i+1;j<arr.length;j++){
          if(duplicate === arr[j]){
              console.log('Duplicate Element :- ',duplicate);
          }
      }
  }

  const arr = [23,12,23,42,78,90,42,42,23];
  
    function findDuplicate(arr){
        const newArray = [];
        for(let i=0;i<arr.length;i++){
            for(let j=i+1;j<arr.length;j++){
                if(!newArray.includes(arr[i])){
                if(arr[i]===arr[j]){
                    newArray[newArray.length] = arr[i];
                }  
                }
            }
        }
        return newArray;
    }  

    console.log(findDuplicate(arr));
------------------------------------------------------------------
- Reverse an array

  //]] Reverse Number without using the .reverse() method
  const arr = [12,23,34,45,56,76];
  console.log("Before Array Reversing :- ",arr);
  let i = 0;
  let j = arr.length - 1;
  while(i<j){
    let temp = arr[i];
    arr[i] = arr[j];
    arr[j] = temp;
    i++;
    j--;
  }
  console.log('After Array Reversing :- ',arr);
------------------------------------------------------------------
- Find max/min element

  //]] Find max/min element
  const arr = [12,42,89,56,43,90,32,2];
  let max = arr[0];
  for(let i=1;i<arr.length;i++){
    if(max>arr[i]){
      max = arr[i]; 
    }
  }
  console.log('Maximum Number in Array :- ',max);


//Find Mix & Max single timing
function findMaxAndMin(arr){
    
    const sortedArray = arr.sort((a,b) => {
        return a - b;
    });
    
    return [sortedArray[0],sortedArray[arr.length-1]];
}

const newArray = findMaxAndMin([12,23,89,67,7,54,90,43,677,90,43]);

console.log('Min Value :- ',newArray[0]);
console.log('Max Value :- ',newArray[1]);

------------------------------------------------------------------
- ]]Remove duplicates

  const arr = [12,12,23,78,90,56,23];
  const uniqueArr = [];
  console.log('Before Array :- ',arr);
  for(let i=0;i<arr.length;i++){
      let isDuplicate = false;
      for(let j=0;j<uniqueArr.length;j++){
          if(arr[i]===uniqueArr[j]){
              isDuplicate = true;
              break;
          }
      }
      if(!isDuplicate){
          uniqueArr[uniqueArr.length] = arr[i];
      }
  }
  console.log('After Array  :- ',uniqueArr);
------------------------------------------------------------------
- Merge two sorted arrays
  
  const arr1 = [34,23,89,90,54,32];
  const arr2 = [908,342,897,903,675,432];
  const newArray = [...arr1,...arr2];
  newArray.sort((a,b)=>{
      return a - b;
  });
  console.log('Merge Two Sorted Arrays :- ',newArray);
------------------------------------------------------------------
- ]]Rotate array by K steps

  const arr = [23,34,45,56,67];
  const rotatedArray = [];
  let k = 2;
  let n = arr.length-1;
  for(let i=k;i<=n;i++){
      rotatedArray[rotatedArray.length] = arr[i];
  }
  for(let i=0;i<k;i++){
      rotatedArray[rotatedArray.length] = arr[i];
  }
  console.log(rotatedArray);
------------------------------------------------------------------
- ]]Find missing number

  const arr = [1,2,3,4,5,6,7,9];
  console.log('Find Missing Value in Array :- ',arr);
  const n = arr.length + 1;
  const totalValue = [n * (n+1)]/2;
  let sumArray = 0;
  for(let a of arr){
      sumArray = sumArray + a;
  }
  console.log('Missing Value in Array :- ',(totalValue-sumArray));
------------------------------------------------------------------
- ]]Find duplicate number

  const arr = [23,12,23,42,78,90,42];
  for(let i=0;i<arr.length;i++){
      let duplicate = arr[i];
      for(let j=i+1;j<arr.length;j++){
          if(duplicate === arr[j]){
              console.log('Duplicate Element :- ',duplicate);
          }
      }
  }

  bug fixing 

  const arr = [23,12,23,42,78,90,42,42,42];
  
  function findDuplicate(arr){
      const newArray = [];
      for(let i=0;i<arr.length;i++){
          for(let j=i+1;j<arr.length;j++){
              if(arr[i]===arr[j]){
                //   console.log(arr[i]);
                if(!newArray.includes(arr[i])){
                    newArray[newArray.length] = arr[i];
                }
              }
          }
      }
      return newArray;
  }
  
  console.log(findDuplicate(arr));
------------------------------------------------------------------
- ]]Move zeros to end
  const arr = [23,0,34,0,45,0,89,0,90];
  console.log('Before :- ',arr);
  const arrZero = [];
  const arrValue = [];
  for(let i=0;i<arr.length;i++){
      if(arr[i]===0){
          arrZero.push(arr[i]);
      }else{ 
          arrValue.push(arr[i]);
      }
  }
  console.log('After :- ',[...arrValue,...arrZero]);
------------------------------------------------------------------
- ]]Two sum problem
  const arr = [2,7,0,11,7,4];
  const target = 9;
  for(let i=0;i<arr.length;i++){
      for(let j=i+1;j<arr.length;j++){
          if(target == arr[i] + arr[j]){
              console.log(`Index :- [${i},${j}]`);
          }
      }
  }
------------------------------------------------------------------
- Best time to buy and sell stock

  // // Function to calculate maximum profit with ONE buy and ONE sell
  // function maxProfit(prices) {
  //   let minPrice = Infinity;  // lowest price seen so far
  //   let maxProfit = 0;        // best profit found so far
  
  //   for (let price of prices) {
  //     // update minimum price if current price is lower
  //     if (price < minPrice) {
  //       minPrice = price;
  //     }
  //     // calculate profit if sold today, update maxProfit if better
  //     maxProfit = Math.max(maxProfit, price - minPrice);
  //   }
  
  //   return maxProfit;
  // }
  
  // // Example usage:
  // console.log(maxProfit([7, 1, 5, 3, 6, 4])); // Output: 5 (buy at 1, sell at 6)
  // console.log(maxProfit([7, 6, 4, 3, 1]));    // Output: 0 (no profit possible)
  
  // Best time to buy and sell stock
  
  function maxProfit(prices){
      // min = infinity;
      // profit = 0;
      // profit = max(profit,price-min);
      let minPrice = Infinity;
      let maxProfit = 0;
      
      for(let price of prices){
          //update mininum price
          if(price<minPrice){
              minPrice = price;
          }
          
          // calculated the maxProfit
          // profit = max(profit,price-min);
          
          maxProfit = Math.max(maxProfit,(price-minPrice));
      }
      return maxProfit;
  }
  
  console.log('Max Profit :- ',maxProfit([7,1,5,3,6,4]));
  console.log('Max Profit :- ',maxProfit([7,2,3,9,8,3,1]));
  
  // min = infinity;
  // profit = 0;
  // profit = max(profit,(price-min));
  
  // Day First : 7
  // price = 7;
  // min = 7;
  // profit = max(0,(7-7)) = 0
  
  // Day Second : 2
  // min = 2
  // profit = max(0,(2-2)) = 0
  
  // Day Third : 3
  // min = 2
  // profit = max(0,(3-2)) = 1
  
  // Day Four : 9
  // min = 2 
  // profit = max(1,(9-2)) = 7
  
  // Day Five : 8 
  // min = 2
  // profit = max(7,(8-2)) = 7
  
  // Day Six : 3 
  // min = 2
  // profit = max(7,(3-2)) = 7
  
  // Day Seven : 1
  // min = 1
  // profit = max(7,(1-1)) = 7
  
  // Day Eight : 9
  // min = 1
  // profit = max(7,(9-1)) = 8
------------------------------------------------------------------

  - ]]Product of array except self
  // Product of array except self
  // [1, 2, 3, 4]
  // [24, 12, 8, 6]
  
  function multiplicationOfItSelf(arr){
      let newArray = [];
      for(let i=0;i<arr.length;i++){
          let s = arr[i];
          let result = 1;
          for(let j=0;j<arr.length;j++){
              if(s!==arr[j]){
                  result = result * arr[j];
              }
          }
          newArray.push(result);
      }
      return newArray;
  }
  
  console.log(multiplicationOfItSelf([1,2,3,4]));
------------------------------------------------------------------

- ]]Combine two or more array
const arr = [[12,23,90,78,4,89],[45,99,1,8,87],[21,32,2]];

const newArray = [];

for(let i=0;i<arr.length;i++){
    for(let j=0;j<arr[i].length;j++){
        newArray.push(arr[i][j]);
    }
}

newArray.sort((a,b)=>a-b);
console.log(newArray);
------------------------------------------------------------------
const statement = "Hello Everyone My Name is Vishal Nimbarte From Pune";

// ]]find the length of Vishal word without using the inbuild function

const arr = [];
let char = '';
for(let i=0;i<statement.length;i++){
    if(statement.charAt(i)!==' '){
        char = char + statement.charAt(i);
    }
    else {
        arr.push(char);
        char = '';
    }
}

for(let i=0;i<arr.length;i++){
   if(arr[i]==='Vishal'){
       console.log(`${arr[i]} -> ${arr[i].length}`);
   }
}

______________________________________________________________________________________________

}}}reverse array without inbuild method
-> 

const arr = [12,34,78,3,9,5,20];

console.log("Before Reverse :- ",arr);

function reverseArray(arr){
	let i = 0;
	let j = arr.length - 1;
	while(i<j){
		let temp = arr[i];
		arr[i] = arr[j];
		arr[j] = temp;
		i++;
		j--;
	}
	return arr;
}

console.log("After Reverse :- ",reverseArray(arr));
______________________________________________________________________________________________ 
}}}prime number find 
->
function isPrime(num){
  if(num<=1) return false;
  for(let i=2;i<num;i++){
    if(num%2===0){
      return false;
    }
  }
  return true;
}

console.log(isPrime(0));
console.log(isPrime(1));
console.log(isPrime(2));
console.log(isPrime(3));
console.log(isPrime(4));
console.log(isPrime(5));
console.log(isPrime(6));
console.log(isPrime(7));
console.log(isPrime(10));
______________________________________________________________________________________________
}}}how many times number here in array find the countes of same number present in array 
->// }}}how many times number here in array find the countes of same number present in array 
// ->

const arr = [12,23,34,45,12,34,23,89,54,90,12];

function countesOfSameNumber(arr){
  let count = {};
  for(let i=0;i<arr.length;i++){
    let num = arr[i];
    if(count[num]){
      count[num]++;
    }
    else{
      count[num] = 1;
    }
  }
  return count;
}

console.log(countesOfSameNumber(arr));
______________________________________________________________________________________________


}}}bubble sort 1D,2D
const arr = [[12, 23, 90, 78, 4, 89], [45, 99, 1, 8, 87], [21, 32, 2]];

console.log('Before Sorted :- ', arr);

function sortArray(arr) {
  for (let i = 0; i < arr.length; i++) {
    for (let j = 0; j < arr.length; j++) {
      if (arr[j] > arr[j + 1]) {
        let temp = arr[j];
        arr[j] = arr[j + 1];
        arr[j + 1] = temp;
      }
    }
  }
  return arr;
}

function sort2DArray(arr) {
  const newArray = [];
  for (let i = 0; i < arr.length; i++) {
    newArray.push(sortArray(arr[i]));
  }
  return newArray;
}

console.log('After Sorted :- ', sort2DArray(arr));
______________________________________________________________________________________________
}}}find maximum number and smallest number in array 
const arr = [23,45,89,90,12,78,44];

function findMaximumNumber(arr){
  let max = arr[0];
  for(let i=1;i<arr.length;i++){
    if(arr[i]<max){
      max = arr[i];
    }
  }
  return max;
}

console.log("Maximum",findMaximumNumber(arr));

------------------------------------------------------------------
- Set matrix zeroes

const matrix = [
    [1, 2, 3],
    [4, 0, 6],
    [7, 8, 9]
];

function setMatrixZeroes(matrix) {
    const rows = matrix.length;
    const cols = matrix[0].length;

    const zeroRows = new Set();
    const zeroCols = new Set();

    // Find zero positions
    for (let i = 0; i < rows; i++) {
        for (let j = 0; j < cols; j++) {
            if (matrix[i][j] === 0) {
                zeroRows.add(i);
                zeroCols.add(j);
            }
        }
    }

    // Set rows to zero
    for (let r of zeroRows) {
        for (let j = 0; j < cols; j++) {
            matrix[r][j] = 0;
        }
    }

    // Set columns to zero
    for (let c of zeroCols) {
        for (let i = 0; i < rows; i++) {
            matrix[i][c] = 0;
        }
    }

    console.log("updated matrix :- ");
    console.log(matrix);
}

setMatrixZeroes(matrix);
------------------------------------------------------------------

- Search in rotated sorted array

    const arr = [1, 3, 5, 7, 9, 11]
    const target = 9;

    function findElementPosition(arr, target) {
        let start = 0;
        let end = arr.length - 1;
        let mid = 0;
        while (start <= end) {
            mid = Math.floor((start + end) / 2);
            if (arr[mid] === target) {
                console.log(`${target} is found in location at ${mid}`);
                return;
            }
            else if (arr[mid] < target) {
                start = mid + 1;
            }
            else {
                end = mid - 1;
            }
        }
    }

    findElementPosition(arr, target);
------------------------------------------------------------------
- 2D bubble Sort Algorithm:- 

    const array = [[12,32,89,4],[8,2,1],[89,12,45,32,70]];

    console.log(array);

    function sort(array){
        for(let i=0;i<array.length;i++){
            for(let j=0;j<array.length-1;j++){
                if(array[i]<array[j]){
                    let temp = array[i];
                    array[i] = array[j];
                    array[j] = temp;
                }
            }
        }
        return array;
    }

    function bubbleSort2D(array){
        const sorted2DArray = [];
        for(let i=0;i<array.length;i++){
            sorted2DArray[i] = sort(array[i]);
        }
        return sorted2DArray;
    }

    console.log(bubbleSort2D(array));
------------------------------------------------------------------
- Find peak element
- Majority element
- Longest consecutive sequence
- Sliding window maximum
- Subarray sum equals K
- Minimum size subarray sum
- Jump game
- Trapping rain water
- Maximum subarray (Kadane’s)
- Sort colors (Dutch flag)
- Merge intervals
- Insert interval
- Spiral matrix


1. Introduction to Arrays
-> Arrays are a collection of data, and they follow indexing. So indexing wise, data is stored inside the array.

example :- 

const arr = [12,23,34,78];

function printAllValues(arr){
    arr.forEach((data)=>{
        console.log(data);
    });
}

printAllValues(arr);
________________________________________________________________________________________________________________________
2. Introduction to 2-D Arrays
-> 2-D Arrays means an array contains two or more arrays inside it. That is called a 2-D array, and values are stored in matrix format.

example :- 
const arr = 
[
    [1,2,3],
    [4,5,6],
    [7,8,9]
];

function printValues(arr){
    for(let i=0;i<arr.length;i++){
        let row = "";
        for(let j=0;j<arr[i].length;j++){
            row = row + arr[i][j] + " ";
        }
        console.log(row);
    }
}

printValues(arr);
________________________________________________________________________________________________________________________
3. Sorting of Array

const arr = [12,32,78,90,43,56,12];

function sortArray(arr){
    for(let i=0;i<arr.length;i++){
        for(let j=i+1;j<arr.length;j++){
            if(arr[i]>arr[j]){
                let temp = arr[i];
                arr[i] = arr[j];
                arr[j] = temp;
            }
        }
    }
    return arr;
}

console.log(sortArray(arr));
________________________________________________________________________________________________________________________
4. Array Rotation

const arr = [12,23,34,45,56];
let k = 2;

console.log(arr);

function rotateArray(arr,k){//left rotation array
    const newArray = [];
    for(let i=k;i<arr.length;i++){
        newArray[newArray.length] = arr[i];
    }
    for(let i=0;i<k;i++){
        newArray[newArray.length] = arr[i];
    }
    return newArray;
}

console.log(rotateArray(arr,k));

------

// 4. Array Rotation

// [ 12, 23, 34, 45, 56 ]
// [ 45, 56, 12, 23, 34 ]

const arr = [12,23,34,45,56];
let k = 2;

console.log(arr);

function rightRotateArray(arr,k){
    const newArray = [];
    for(let i=arr.length-k;i<arr.length;i++){//i=5-2=3
        newArray[newArray.length] = arr[i];
    }
    for(let j=0;j<arr.length-k;j++){
        newArray[newArray.length] = arr[j];
    }
    return newArray;
}

console.log(rightRotateArray(arr,k));
________________________________________________________________________________________________________________________
5. Reverse an Array or String
->Reverse an Array 

const arr = [12,23,34,45,56,67];

console.log(arr);

function reverseArray(arr){
    let i = 0;
    let j = arr.length - 1;
    while(i<j){
        let temp = arr[i];
        arr[i] = arr[j];
        arr[j] = temp;
        i++;
        j--;
    }
    return arr;
}

console.log(reverseArray(arr));

->Reverse String

let str = 'Hello';

console.log(str);

function reverseString(str){
    let reverse = "";
    for(let i=str.length-1;i>=0;i--){
        reverse = reverse + str.charAt(i);
    }
    return reverse;
}

console.log(reverseString(str));
________________________________________________________________________________________________________________________
6. Find Pairs in Array with Given Sum
->
________________________________________________________________________________________________________________________
7. Sort the Array in Waveform
8. Majority Element in Array
9. Boyer-Moore’s Voting Algorithm
10. K-Pairs with Smallest Sum in 2 Arrays
11. Largest Sum Contiguous SubArray
12. Maximum Average Sub-array of K Length
13. Size of Sub-array with Max Sum
14. Sub-array with Given Sum
15. Triplet that Sum to a Given Value
16. Segregate 0’s and 1’s in Array
17. Segregate 0’s, 1’s and 2’s in Array
18. Sort Elements in Array by Frequency
19. Finding Pythagorean Triplets in an Array
20. Reorder Array using Given Indexes
21. Merging Two Sorted Arrays
22. Minimum Number of Merge Operations to Make an Array Palindrome
23. Find Zeros to be Flipped so that Number of Consecutive 1’s is Maximized










---

## 🔗 Linked Lists (25+ Questions)
- Create a linked list

    let head = null;

    function add(data) {
        if (head === null) {
            head = { data, next: null };
            return;
        }

        let temp = head;
        while (temp.next) {
            temp = temp.next;
        }

        temp.next = { data, next: null };
    }

    function print() {
        let temp = head;
        let result = "";
        while (temp) {
            result += temp.data + " -> ";
            temp = temp.next;
        }
        console.log(result + "null");
    }

    add(10);
    add(20);
    add(30);

    print();
_____________________________________________________________________________________
- Reverse a linked list
_____________________________________________________________________________________
- Detect cycle
_____________________________________________________________________________________
- Find middle node
_____________________________________________________________________________________
- Merge two sorted lists
_____________________________________________________________________________________
- Remove N-th node from end
_____________________________________________________________________________________
- Intersection of two lists
_____________________________________________________________________________________
- Palindrome linked list
_____________________________________________________________________________________
- Flatten multilevel list
_____________________________________________________________________________________
- Copy list with random pointer
_____________________________________________________________________________________
- Add two numbers
_____________________________________________________________________________________
- Rotate list
_____________________________________________________________________________________
- Partition list
_____________________________________________________________________________________
- Reorder list
_____________________________________________________________________________________
- Swap nodes in pairs
_____________________________________________________________________________________
- Delete node in constant time
_____________________________________________________________________________________
- Sort linked list
_____________________________________________________________________________________
- Merge K sorted lists
_____________________________________________________________________________________
- LRU cache
_____________________________________________________________________________________
- Linked list cycle II
_____________________________________________________________________________________
- Odd-even linked list
_____________________________________________________________________________________
- Remove duplicates
_____________________________________________________________________________________
- Convert binary number to integer
_____________________________________________________________________________________
- Split linked list in parts
_____________________________________________________________________________________
- Reverse nodes in k-group
_____________________________________________________________________________________
- Intersection of cycle lists
_____________________________________________________________________________________

---

## 🧱 Stacks & Queues (25+ Questions)
- Implement stack
- Implement queue
- Min stack
- Valid parentheses
- Next greater element
- Evaluate postfix
- Infix to postfix
- Daily temperatures
- Largest rectangle in histogram
- Sliding window maximum
- Implement circular queue
- Stack using queues
- Queue using stacks
- Design browser history
- Decode string
- Remove adjacent duplicates
- Simplify path
- Asteroid collision
- Implement monotonic stack
- Implement deque
- Implement LRU cache
- Implement LFU cache
- Design hit counter
- Implement bounded blocking queue
- Implement priority queue

---

## 🌳 Trees & BSTs (25+ Questions)
- Inorder traversal
- Preorder traversal
- Postorder traversal
- Level order traversal
- Height of tree
- Diameter of tree
- Check if symmetric
- Check if balanced
- Lowest common ancestor
- Validate BST
- Search in BST
- Insert/delete in BST
- Convert sorted array to BST
- Flatten binary tree
- Tree to doubly linked list
- Path sum
- Maximum path sum
- Zigzag level order
- Vertical order traversal
- Serialize/deserialize tree
- Construct tree from preorder/inorder
- Construct tree from postorder/inorder
- Count nodes in complete tree
- Kth smallest in BST
- Recover BST

---

## 🔗 Graphs (25+ Questions)
- DFS traversal
- BFS traversal
- Detect cycle in undirected graph
- Detect cycle in directed graph
- Topological sort
- Clone graph
- Number of islands
- Surrounded regions
- Pacific Atlantic water flow
- Shortest path in binary matrix
- Dijkstra’s algorithm
- Bellman-Ford algorithm
- Floyd-Warshall algorithm
- Kruskal’s MST
- Prim’s MST
- Tarjan’s algorithm
- Kosaraju’s algorithm
- Word ladder
- Course schedule
- Minimum spanning tree
- Bipartite graph check
- Bridges in graph
- Articulation points
- Graph coloring
- Network delay time

---

## 🔐 Heaps & Hashing (25+ Questions)
- Implement min/max heap
- Heapify array
- Kth largest element
- Kth smallest element
- Merge K sorted lists
- Top K frequent elements
- Median in data stream
- Sliding window maximum
- Frequency sort
- Reorganize string
- Sort characters by frequency
- Design Twitter
- Design search autocomplete
- Two sum
- Group anagrams
- Longest consecutive sequence
- Subarray sum equals K
- Count distinct elements
- Find duplicates
- LRU cache
- LFU cache
- Randomized set
- Insert/delete/getRandom
- Longest substring with K distinct
- Longest repeating character replacement

---

## 🧠 Dynamic Programming (25+ Questions)
- Fibonacci numbers
- Climbing stairs
- House robber
- House robber II
- Longest common subsequence
- Longest increasing subsequence
- Edit distance
- 0/1 Knapsack
- Coin change
- Partition equal subset sum
- Palindromic substrings
- Longest palindromic substring
- Decode ways
- Jump game
- Minimum path sum
- Unique paths
- Interleaving string
- Matrix chain multiplication
- Maximum product subarray
- Subset sum
- Rod cutting
- Egg dropping puzzle
- Wildcard matching
- Regular expression matching
- Cherry pickup

---

## 🔤 Strings (25+ Questions)
- Reverse string
- Palindrome check
- Longest palindromic substring
- Longest common prefix
- Group anagrams
- Valid anagram
- Implement strstr
- Count and say
- String compression
- Remove adjacent duplicates
- Decode string
- Multiply strings
- Add binary
- Longest substring without repeating
- Minimum window substring
- Check permutation in string
- Word break
- Regular expression matching
- Wildcard matching
- Z-algorithm
- KMP algorithm
- Rabin-Karp algorithm
- Suffix array
- Trie insert/search
- Auto-complete system

---

## 🧮 Bit Manipulation & Math (25+ Questions)
- Count set bits
- Power of two
- XOR of all elements
- Find missing number
- Single number
- Single number II
- Sum of two integers
- Reverse bits
- Number of 1 bits
- Bitwise AND of numbers range
- Divide two integers
- Excel sheet column number
- GCD and LCM
- Sieve of Eratosthenes
- Fast exponentiation
- Modular inverse
- Prime factorization
- Check if number is power of 4
- Add without arithmetic operators
- Find two non-repeating elements
- Bitmasking for subsets
- Count bits from 1 to N
- Gray code
- Binary representation of decimal
- XOR swap trick

---

Let me know if you’d like this as a printable sheet, flashcards, or a quiz to test your knowledge!
